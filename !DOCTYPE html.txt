<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عالمنا الخاص ❤️</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap');

        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Cairo', sans-serif;
            overflow: hidden;
            background: #ffdeeb;
        }

        /* خلفية رومانسية متحركة */
        .bg-container {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(45deg, #ff9a9e 0%, #fad0c4 99%, #fad0c4 100%);
            z-index: -1;
        }

        .heart-bg {
            position: absolute;
            color: rgba(255, 255, 255, 0.6);
            animation: float 6s infinite linear;
            font-size: 20px;
        }

        @keyframes float {
            0% { transform: translateY(100vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
        }

        /* شاشة الدخول */
        #login-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(255, 255, 255, 0.9);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 100;
            transition: 0.5s;
        }

        .login-box {
            text-align: center;
            padding: 40px;
            background: white;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(255, 77, 109, 0.2);
            border: 2px solid #ff4d6d;
        }

        .login-box h2 { color: #ff4d6d; margin-bottom: 20px; }

        input {
            padding: 15px;
            width: 80%;
            border: 2px solid #ffccd5;
            border-radius: 15px;
            outline: none;
            text-align: center;
            font-size: 18px;
            transition: 0.3s;
        }

        input:focus { border-color: #ff4d6d; }

        button {
            margin-top: 20px;
            padding: 12px 40px;
            background: #ff4d6d;
            color: white;
            border: none;
            border-radius: 25px;
            font-size: 18px;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover { background: #c9184a; transform: scale(1.05); }

        /* المحتوى الرئيسي */
        #main-content {
            display: none;
            text-align: center;
            padding-top: 10vh;
        }

        .love-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            display: inline-block;
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            color: #590d22;
        }

        .timer-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-top: 20px;
        }

        .timer-unit {
            background: #ff4d6d;
            color: white;
            padding: 10px;
            border-radius: 10px;
            font-size: 18px;
        }

        .timer-unit span { display: block; font-size: 25px; font-weight: bold; }
    </style>
</head>
<body>

    <div class="bg-container" id="bg"></div>

    <div id="login-overlay">
        <div class="login-box">
            <div style="font-size: 50px;">🔑</div>
            <h2>Enter Our Secret Code</h2>
            <input type="password" id="passInput" placeholder="اكتبي كلمة السر هنا...">
            <br>
            <button onclick="checkAccess()">Unlock Love ❤️</button>
        </div>
    </div>

    <div id="main-content">
        <div class="love-card">
            <h1 style="font-size: 35px;">بقالنا سوا يا روحي ❤️</h1>
            <div class="timer-grid" id="timer">
                </div>
            <p style="margin-top: 20px; font-size: 20px; color: #ff4d6d;">وعقبال العمر كله مع بعض ✨</p>
        </div>
    </div>

    <script>
        // كلمة السر اللي طلبتها
        function checkAccess() {
            const pass = document.getElementById('passInput').value;
            if (pass === 'nonty') {
                document.getElementById('login-overlay').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('login-overlay').style.display = 'none';
                    document.getElementById('main-content').style.display = 'block';
                    startCounter();
                }, 500);
            } else {
                alert("الكود غلط يا عيوني.. ركزي 😉");
            }
        }

        // حساب تاريخ البداية بناءً على المدة اللي ذكرتها
        // (سنة و 10 شهور و 23 يوم و 17 ساعة و 46 دقيقة و 10 ثواني من الآن)
        const now = new Date();
        const startDate = new Date();
        startDate.setFullYear(now.getFullYear() - 1);
        startDate.setMonth(now.getMonth() - 10);
        startDate.setDate(now.getDate() - 23);
        startDate.setHours(now.getHours() - 17);
        startDate.setMinutes(now.getMinutes() - 46);
        startDate.setSeconds(now.getSeconds() - 10);

        function startCounter() {
            setInterval(() => {
                const currentTime = new Date();
                const diff = currentTime - startDate;

                const days = Math.floor(diff / (1000 * 60 * 60 * 24));
                const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
                const mins = Math.floor((diff / 1000 / 60) % 60);
                const secs = Math.floor((diff / 1000) % 60);

                // حساب تقريبي للسنين والشهور من الأيام
                const years = Math.floor(days / 365);
                const remainingDays = days % 365;
                const months = Math.floor(remainingDays / 30);
                const finalDays = remainingDays % 30;

                document.getElementById('timer').innerHTML = `
                    <div class="timer-unit"><span>${years}</span> سنة</div>
                    <div class="timer-unit"><span>${months}</span> شهر</div>
                    <div class="timer-unit"><span>${finalDays}</span> يوم</div>
                    <div class="timer-unit"><span>${hours}</span> ساعة</div>
                    <div class="timer-unit"><span>${mins}</span> دقيقة</div>
                    <div class="timer-unit"><span>${secs}</span> ثانية</div>
                `;
            }, 1000);
        }

        // إنشاء قلوب متساقطة في الخلفية
        function createHearts() {
            const bg = document.getElementById('bg');
            const icons = ['❤️', '🌸', '💖', '🌹', '✨'];
            setInterval(() => {
                const heart = document.createElement('div');
                heart.className = 'heart-bg';
                heart.style.left = Math.random() * 100 + 'vw';
                heart.style.animationDuration = (Math.random() * 3 + 3) + 's';
                heart.innerText = icons[Math.floor(Math.random() * icons.length)];
                bg.appendChild(heart);
                setTimeout(() => heart.remove(), 6000);
            }, 300);
        }
        createHearts();
    </script>
</body>
</html>